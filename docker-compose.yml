version: "3.9"

services:
  django:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
    - "8000:8000"
    volumes:
    - .:app/:rw

  rabbitmq:
    image: "rabbitmq"
    ports:
    - "5672:5672"

  celery: &django
